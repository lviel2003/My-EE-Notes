# FFT

This is a test note for my FFT notes.

## Main idea

The FFT is just a faster way to compute the DFT.

$$X[k]=\sum_{n=0}^{N-1}x[n]e^{-j\frac{2\pi}{N}kn}$$

> [!note]
> This is a test Obsidian callout.

Go back to [[index]].

Note on how to push: 
git add .
git commit -m "Message"
git push

Note on how to run the server:
npx quartz build --serve

